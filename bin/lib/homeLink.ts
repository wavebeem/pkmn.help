// Shared "back to the app" link for standalone static pages (changelog,
// licenses, credits) that live outside the bundle and aren't part of the app's
// own router.
export const homeLink = `<p><a href="/">Back to PKMN.help</a></p>`;
